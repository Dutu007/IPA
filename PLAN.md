# 智析病理 — 项目计划

## 应用形态

**本地 Web 服务 + 浏览器前端**：用户在本地启动服务后通过浏览器访问，WSI 文件无需上传云端，数据安全且响应快。

- 开发阶段：`python main.py` → 浏览器打开 `localhost:8000`
- 交付阶段：`start.bat` 一键启动，也可用 PyInstaller 打包为 .exe

## 用户交互流程

```
┌─────────────────────────────────────────┐
│  1. 输入 WSI + 文字描述                   │
│     ┌──────────┐  ┌──────────────────┐  │
│     │ 选择文件  │  │ 病理描述文字      │  │
│     │ *.svs/   │  │ "乳腺组织..."    │  │
│     │ *.tiff   │  │                  │  │
│     └──────────┘  └──────────────────┘  │
│                                          │
│  2. 点击"开始分析"                        │
│     [========进度条========]  45%         │
│     Step1: QC检测 ✅                     │
│     Step2: ROI选取 🔄                    │
│     Step3: 精细分析 ⏳                   │
│                                          │
│  3. 查看综合报告                          │
│     ├── QC 统计: 合格率 92%               │
│     ├── ROI 热力图 + 候选区域             │
│     ├── 精细分析结果                      │
│     └── 文字描述关联分析                  │
└─────────────────────────────────────────┘
```

## 总体流程

```
用户输入（WSI + 文字描述）
    │
    ▼
Step 1: 初步切割 + 质量检测
    │  lazyslide + pathprofilerQC2
    ▼
Step 2: ROI 选取
    │  YOLOv8 病变/非病变检测
    ▼
Step 3: 精细分析
    │  (待定)
    ▼
综合分析报告 → 前端展示
```

---

## Step 1 🚧 WSI 初步切割与质量检测

### 目标
对 WSI 进行初步分块，同时用质量检测模型过滤掉低质量/无效区域（模糊、空白、气泡、折叠等）。

### 工具
- **lazyslide**：WSI 读取、分块管理
- **pathprofilerQC2**：集成在 lazyslide 中的病理切片质量评估模型

### 流程
```
WSI → lazyslide 读取 → 分块（如 256×256）
    → pathprofilerQC2 质量评分 → 过滤低质量块
    → 输出合格瓦片
```

### 输出
```
output/<session_id>/step1/
├── tiles/          ← 通过质检的瓦片图像
├── rejected/       ← 被拒绝的低质量瓦片（可选保留）
└── qc_report.json  ← 每块质量评分 + 统计摘要
```

---

## Step 2 🚧 ROI 选取

### 2.1 数据集准备 ✅

基于 PanNuke 构造 YOLOv8 二分类数据集：
- `lesion`（病变）/ `non_lesion`（非病变）
- 7901 张 256×256 图像，189,744 个 bbox
- 输出：`pannuke_yolo/`（train/val/test）

### 2.2 YOLOv8 模型训练（待进行）

- **模型**：yolov8n（验证）→ yolov8s（正式）
- **训练**：`yolo detect train data=data.yaml model=yolov8n.pt epochs=50 imgsz=256`
- **重点指标**：lesion 类 recall（避免漏检）

### 2.3 ROI 切割策略

对 WSI 做滑动窗口推理，按病变密度筛选：

| 参数 | 建议值 | 说明 |
|------|--------|------|
| 窗口大小 | 256×256 | 与训练尺寸一致 |
| 步长 | 128~200 | 平衡精度与效率 |
| 密度阈值 | ≥3 lesion/窗口 | 过滤低信息量区域 |

切割方案：**密度热力图粗筛 → 检测框聚类精细裁剪**

### 2.4 输出

```
output/<session_id>/step2/
├── heatmap.png     ← WSI 级病变密度热力图
├── regions/        ← 裁剪后的 ROI 图像
└── metadata.json   ← 位置、密度、lesion 数量
```

---

## Step 3 📋 精细分析（待定）

接收 Step 2 的候选 ROI 做进一步分析：
- 病变亚型细分
- 形态学特征提取
- 结合用户输入的文字描述做关联分析

具体方案待 Step 2 完成后确定。

---

## 报告整合

三步结果汇总为一个综合报告，包含：
- QC 质量评估摘要
- ROI 可视化（热力图 + 候选区域标注）
- 精细分析结论
- 与用户文字描述的关联解读

---

## 技术栈

| 环节 | 工具 |
|------|------|
| 应用架构 | FastAPI 本地服务 + 浏览器前端 |
| WSI 读取与分块 | lazyslide |
| 质量检测 | pathprofilerQC2 |
| 目标检测训练/推理 | Ultralytics YOLOv8 |
| 前端 | Vue 3 + Element Plus |
| 任务管理 | Celery + Redis |
| 数据管理 | pandas, NumPy |
| 交付 | start.bat / PyInstaller 打包 |
